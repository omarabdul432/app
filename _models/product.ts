export interface Product {
    id: string,
    imageURL: string,
    title: string,
    price: string
}

export interface Productcategory {
    id: string,
    imageURL: string,
    title: string,
    price: string,
    category: string
}
